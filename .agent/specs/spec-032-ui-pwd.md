# UI E2E 测试设计 — 密码管理 (PWD)

> **SPEC-032** | Status: 设计中 | Date: 2026-07-09 | Phase: P8

## 1. 目标

定义 DataAgent 密码管理模块的 E2E UI 测试用例规范。覆盖初始密码登录后横幅通知、修改密码页、成功修改密码、旧密码错误、新密码不一致、新密码强度校验和所有角色均可修改自己密码。

> **设计依据**: `docs/DataAgent-UI测试用例文档.md` §19
>
> **测试框架**: Playwright (TypeScript)
>
> **用例范围**: UI-149 ~ UI-155（共 7 个用例）

## 2. 前置依赖检查

| 前置 Spec | 状态 | 备注 |
|-----------|:---:|------|
| SPEC-003 | ✅ | 基础设施（密码加密、Vault） |
| SPEC-013 | ✅ | 管理后台（密码管理页面定义） |
| SPEC-014 | ✅ | 测试体系（Playwright 框架） |
| SPEC-017 | ✅ | AUTH |

## 3. 测试范围

| 子功能 | 用例 | 优先级 |
|--------|------|:------:|
| 初始密码登录后横幅通知 | UI-149 | P0 |
| 修改密码页 | UI-150 | P0 |
| 成功修改密码 | UI-151 | P0 |
| 旧密码错误 | UI-152 | P0 |
| 新密码不一致 | UI-153 | P0 |
| 新密码强度校验 | UI-154 | P1 |
| 所有角色均可修改自己密码 | UI-155 | P1 |

## 4. 测试用例

### UI-149: Pwd — 初始密码登录后横幅通知

- **优先级**: P0
- **前置条件**: 使用 system_admin 初始随机密码登录（`password_changed=false`）
- **步骤**:
  1. 登录系统
  2. 检查页面顶部
- **预期结果**:
  - 后台头部显示横幅通知（黄色或琥珀色背景）
  - 内容：「您正在使用系统初始密码，请尽快修改」
  - 点击通知可跳转到修改密码页
  - `data-testid`: `pwd-initial-banner`, `pwd-change-link`

### UI-150: Pwd — 修改密码页

- **优先级**: P0
- **前置条件**: 已登录
- **步骤**:
  1. 导航到修改密码页面
- **预期结果**:
  - 页面包含：旧密码输入框 + 新密码输入框 + 确认新密码输入框
  - 所有输入框为密码类型（掩码显示）
  - 「确认修改」按钮
  - `data-testid`: `pwd-old-input`, `pwd-new-input`, `pwd-confirm-input`, `pwd-change-btn`

### UI-151: Pwd — 成功修改密码

- **优先级**: P0
- **前置条件**: 修改密码页
- **步骤**:
  1. 输入正确的旧密码
  2. 输入新密码（满足强度要求：至少 8 位，含大小写字母+数字）
  3. 确认新密码一致
  4. 点击确认
- **预期结果**:
  - 成功 toast：「密码修改成功，请使用新密码重新登录」
  - 自动登出，跳转到登录页
  - 初始密码横幅不再出现（`password_changed=true`）
  - 旧密码无法再登录
  - `data-testid`: `pwd-change-success-toast`

### UI-152: Pwd — 旧密码错误

- **优先级**: P0
- **前置条件**: 修改密码页
- **步骤**:
  1. 输入错误的旧密码
  2. 点击确认
- **预期结果**:
  - 错误提示：「旧密码不正确」
  - 密码未被修改
  - `data-testid`: `pwd-old-error`

### UI-153: Pwd — 新密码不一致

- **优先级**: P0
- **前置条件**: 修改密码页
- **步骤**:
  1. 输入正确旧密码
  2. 输入新密码 `NewPass1`
  3. 确认新密码输入 `NewPass2`（不一致）
  4. 点击确认
- **预期结果**:
  - 确认密码输入框下方显示：「两次输入的密码不一致」
  - 密码未被修改
  - `data-testid`: `pwd-confirm-error`

### UI-154: Pwd — 新密码强度校验

- **优先级**: P1
- **前置条件**: 修改密码页
- **步骤**:
  1. 输入弱密码 `123456`
  2. 点击确认
- **预期结果**:
  - 显示提示：「密码至少 8 位，需包含大小写字母和数字」
  - 密码未被修改
  - `data-testid`: `pwd-new-error`

### UI-155: Pwd — 所有角色均可修改自己密码

- **优先级**: P1
- **前置条件**: 以 user 角色登录
- **步骤**:
  1. 检查是否可访问修改密码页
  2. 执行修改密码流程
- **预期结果**:
  - user/admin/system_admin 均可访问修改密码页
  - 修改密码功能正常
  - `data-testid`: N/A

## 5. 可行性分析

| 检查项 | 结论 |
|--------|------|
| 是否需要模拟后端 API | Yes — 密码修改 API |
| 是否依赖第三方服务 | No |
| 是否需要特殊测试数据 | Yes — 初始密码状态的账号 mock |
| 是否需要新增 DB 集合 | No |
| 是否影响现有 API | No |
| 性能影响 | 无 |
| 实现复杂度 | Low-Medium — 标准表单验证 + 密码修改流程 |
| 是否有无法实现的用例 | **待确认** — `UI-151`「旧密码无法再登录」需要跨 test 验证（修改后使用旧密码尝试登录） |

## 6. 相关文件

| File | Role | Change Magnitude |
|------|------|:---:|
| `tests/ui/pwd.spec.ts` | PWD E2E 测试实现 | New |
| `tests/ui/fixtures/pwd.fixture.ts` | Pwd mock 数据 fixture | New |

## 7. 验证标准

- [ ] 所有 P0 用例（UI-149 ~ UI-153）必须通过
- [ ] 所有 P1 用例（UI-154, UI-155）必须通过
- [ ] 密码修改 UI 100% 符合 SPEC-013 §3 和 SPEC-003 §6.3

## 8. UI Test / E2E 验收规则

- [ ] **必须** mock 后端 Password API
- [ ] **必须** 验证密码输入框的 password 类型和掩码显示
- [ ] **必须** 测试所有错误路径（旧密码错误、新密码不一致、强度不足）
- [ ] **必须** 验证成功修改后的自动登出
- [ ] **严禁** 使用真实用户凭据

参考: `.agent/memory/E2E_TESTING.md`
