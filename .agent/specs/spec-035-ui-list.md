# UI E2E 测试设计 — 列表管理通用规范 (LIST)

> **SPEC-035** | Status: 设计中 | Date: 2026-07-09 | Phase: P8

## 1. 目标

定义 DataAgent 列表管理通用规范的 E2E UI 测试用例规范。覆盖分页控件默认值、页码跳转、每页条数切换不重置筛选、表头排序（升序/降序/默认）和全选/取消全选。

> **设计依据**: `docs/DataAgent-UI测试用例文档.md` §22
>
> **测试框架**: Playwright (TypeScript)
>
> **用例范围**: UI-167 ~ UI-171（共 5 个用例）

> **说明**: 本 Spec 中的测试用例为跨所有管理页面的**通用列表交互规范**，实际测试可在任意一个数据量充足的列表页上执行（建议选择用户管理或审计日志页）。

## 2. 前置依赖检查

| 前置 Spec | 状态 | 备注 |
|-----------|:---:|------|
| SPEC-013 | ✅ | 管理后台（列表管理通用规范定义） |
| SPEC-014 | ✅ | 测试体系（Playwright 框架） |
| SPEC-017 | ✅ | AUTH |

## 3. 测试范围

| 子功能 | 用例 | 优先级 |
|--------|------|:------:|
| 分页控件默认值 | UI-167 | P0 |
| 页码跳转 | UI-168 | P1 |
| 每页条数切换不重置筛选 | UI-169 | P0 |
| 表头排序（升序/降序/默认） | UI-170 | P0 |
| 全选/取消全选 | UI-171 | P1 |

## 4. 测试用例

### UI-167: List — 分页控件默认值

- **优先级**: P0
- **前置条件**: 任何列表页（用户管理/任务管理/审计日志/Agent列表），数据量 > 20
- **步骤**:
  1. 检查分页控件
- **预期结果**:
  - 默认每页 20 条
  - 显示总条数和总页数
  - 页码导航：上一页 / 数字页码 / 下一页
  - 每页条数切换：10 / 20 / 50 / 100 下拉
  - `data-testid`: `{page}-pagination`, `{page}-page-size-select`
- **相关 PRD**: PRD §F-19

### UI-168: List — 页码跳转

- **优先级**: P1
- **前置条件**: 数据量 > 40（≥ 3 页）
- **步骤**:
  1. 点击「下一页」
  2. 点击第 3 页
  3. 点击「上一页」
- **预期结果**:
  - 翻页后数据更新为对应页的数据
  - URL 参数反映当前页码
  - 上一页在首页时禁用
  - 下一页在末页时禁用
  - `data-testid`: `{page}-pagination-prev`, `{page}-pagination-next`, `{page}-pagination-page-3`

### UI-169: List — 每页条数切换不重置筛选

- **优先级**: P0
- **前置条件**: 已应用筛选条件
- **步骤**:
  1. 设置状态筛选为「运行中」
  2. 切换每页条数从 20 到 50
- **预期结果**:
  - 筛选条件保持「运行中」
  - 仅改变每页显示数量
  - `data-testid`: `{page}-page-size-select`

### UI-170: List — 表头排序（升序/降序/默认）

- **优先级**: P0
- **前置条件**: 任何表格列表页
- **步骤**:
  1. 点击「创建时间」表头 → 变为降序 ↓
  2. 再次点击 → 变为升序 ↑
  3. 第三次点击 → 恢复默认排序
- **预期结果**:
  - 首次点击：降序排列，表头显示 ↓ 指示器
  - 第二次：升序排列，表头显示 ↑ 指示器
  - 第三次：恢复默认，指示器消失
  - 排序方向变化不影响筛选条件
  - `data-testid`: `{page}-table-header-{col}`, `{page}-sort-indicator-{col}`

### UI-171: List — 全选/取消全选

- **优先级**: P1
- **前置条件**: 支持批量操作的列表页
- **步骤**:
  1. 点击表头 checkbox（全选）
  2. 点击「取消全选」
- **预期结果**:
  - 全选：当前页所有行被选中
  - 取消：所有行取消选中
  - 显示选中数量「已选 N 项」
  - `data-testid`: `{page}-select-all`, `{page}-select-count`

## 5. 可行性分析

| 检查项 | 结论 |
|--------|------|
| 是否需要模拟后端 API | Yes — 列表分页/排序 API |
| 是否依赖第三方服务 | No |
| 是否需要特殊测试数据 | Yes — 大数据量 mock（>40 条） |
| 是否需要新增 DB 集合 | No |
| 是否影响现有 API | No |
| 性能影响 | 无 |
| 实现复杂度 | Medium — 需要 mock 分页/排序参数变化和相应数据返回 |
| 是否有无法实现的用例 | **待确认** — `UI-168` URL 参数反映页码（需要路由支持）；`UI-169` 筛选条件保持需要 mock 两次 API 请求并验证参数一致性 |

## 6. 相关文件

| File | Role | Change Magnitude |
|------|------|:---:|
| `tests/ui/list.spec.ts` | LIST E2E 测试实现 | New |
| `tests/ui/fixtures/list.fixture.ts` | List mock 数据 fixture | New |

## 7. 验证标准

- [ ] 所有 P0 用例（UI-167, UI-169, UI-170）必须通过
- [ ] 所有 P1 用例（UI-168, UI-171）必须通过
- [ ] 列表交互 UI 100% 符合 PRD §F-19 通用规范

## 8. UI Test / E2E 验收规则

- [ ] **必须** mock 后端列表 API（带分页/排序参数）
- [ ] **必须** 验证分页切换后 URL 参数变化
- [ ] **必须** 验证排序指示器 UI（↑/↓）的出现和消失
- [ ] **必须** 验证筛选条件在分页切换后保持
- [ ] **严禁** 使用真实数据库进行分页测试

参考: `.agent/memory/E2E_TESTING.md`
