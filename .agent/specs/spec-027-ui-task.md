# UI E2E 测试设计 — 任务管理（全局）(TASK)

> **SPEC-027** | Status: 设计中 | Date: 2026-07-09 | Phase: P8

## 1. 目标

定义 DataAgent 全局任务管理模块的 E2E UI 测试用例规范。覆盖任务管理页渲染、全局查看所有用户任务、查看任务详情、取消运行中任务、重试失败任务和批量取消任务。

> **设计依据**: `docs/DataAgent-UI测试用例文档.md` §14
>
> **测试框架**: Playwright (TypeScript)
>
> **用例范围**: UI-109 ~ UI-114（共 6 个用例）

## 2. 前置依赖检查

| 前置 Spec | 状态 | 备注 |
|-----------|:---:|------|
| SPEC-009 | ✅ | 任务队列与调度（任务全局管理） |
| SPEC-013 | ✅ | 管理后台（任务管理页面定义） |
| SPEC-014 | ✅ | 测试体系（Playwright 框架） |
| SPEC-017 | ✅ | AUTH |
| SPEC-018 | ✅ | LAYOUT |
| SPEC-020 | ✅ | AGENT（任务生命周期相关） |

## 3. 测试范围

| 子功能 | 用例 | 优先级 |
|--------|------|:------:|
| 任务管理页渲染 | UI-109 | P0 |
| 全局查看所有用户任务 | UI-110 | P0 |
| 查看任务详情 | UI-111 | P0 |
| 取消运行中任务 | UI-112 | P0 |
| 重试失败任务 | UI-113 | P1 |
| 批量取消任务 | UI-114 | P1 |

## 4. 测试用例

### UI-109: Task — 任务管理页渲染

- **优先级**: P0
- **前置条件**: 以管理员身份登录，点击「任务管理」
- **步骤**:
  1. 检查页面
- **预期结果**:
  - Header：「任务管理」
  - 筛选标签：运行中(8) / 已完成 / 失败
  - 默认选中「运行中」
  - 任务表格：任务名称 / 状态 / 类型 / 发起人 / 创建时间 / 操作
  - `data-testid`: `task-mgmt-page-header`, `task-mgmt-filter-tabs`, `task-mgmt-table`
- **相关设计**: UI原型设计文档 §3.9

### UI-110: Task — 全局查看所有用户任务

- **优先级**: P0
- **前置条件**: 任务管理页，有其他用户创建的任务
- **步骤**:
  1. 检查任务表格数据
- **预期结果**:
  - 表格中显示所有用户的任务（非仅自己创建）
  - 发起人列显示各任务的创建者
  - `data-testid`: `task-mgmt-row-{taskId}`, `task-mgmt-creator-{taskId}`

### UI-111: Task — 查看任务详情

- **优先级**: P0
- **前置条件**: 任务列表中有任务
- **步骤**:
  1. 点击某任务的「查看」按钮
- **预期结果**:
  - 展开任务详情面板（与 Agent 模式的任务详情一致）
  - 或有独立路由的任务详情页
  - 显示进度、日志、Artifact
  - `data-testid`: `task-mgmt-view-btn-{taskId}`

### UI-112: Task — 取消运行中任务

- **优先级**: P0
- **前置条件**: 有 status=running 的任务
- **步骤**:
  1. 点击运行中任务的「取消」按钮
  2. 确认
- **预期结果**:
  - 弹出确认弹窗
  - 确认后任务状态变为「已取消」
  - `data-testid`: `task-mgmt-cancel-btn-{taskId}`

### UI-113: Task — 重试失败任务

- **优先级**: P1
- **前置条件**: 有 status=failed 的任务
- **步骤**:
  1. 点击失败任务的「重试」按钮
- **预期结果**:
  - 任务重新入队
  - 状态变为「排队中」
  - `data-testid`: `task-mgmt-retry-btn-{taskId}`

### UI-114: Task — 批量取消任务

- **优先级**: P1
- **前置条件**: 筛选标签「运行中」，有 ≥ 2 个任务
- **步骤**:
  1. 勾选多个任务的 checkbox
  2. 点击「批量取消」
  3. 确认
- **预期结果**:
  - 出现「批量操作」工具条
  - 显示已选任务数
  - 确认后所有选中任务变为「已取消」
  - `data-testid`: `task-mgmt-batch-select`, `task-mgmt-batch-cancel-btn`

## 5. 可行性分析

| 检查项 | 结论 |
|--------|------|
| 是否需要模拟后端 API | Yes — 全局任务列表 API、取消/重试 API |
| 是否依赖第三方服务 | No |
| 是否需要特殊测试数据 | Yes — 来自不同用户的任务 mock 数据 |
| 是否需要新增 DB 集合 | No |
| 是否影响现有 API | No |
| 性能影响 | 无 |
| 实现复杂度 | Low-Medium — 复用 AGENT spec 中的任务 mock 模式，增加多用户和批量操作 |
| 是否有无法实现的用例 | **待确认** — `UI-114` 批量操作工具条的出现/消失逻辑 |

## 6. 相关文件

| File | Role | Change Magnitude |
|------|------|:---:|
| `tests/ui/task.spec.ts` | TASK E2E 测试实现 | New |
| `tests/ui/fixtures/task.fixture.ts` | Task mock 数据 fixture | New |

## 7. 验证标准

- [ ] 所有 P0 用例（UI-109 ~ UI-112）必须通过
- [ ] 所有 P1 用例（UI-113, UI-114）必须通过
- [ ] 全局任务管理 UI 100% 符合 UI 原型设计文档 §3.9
- [ ] 批量操作正确

## 8. UI Test / E2E 验收规则

- [ ] **必须** mock 后端 Task Management API
- [ ] **必须** 验证全局视图包含所有用户的任务
- [ ] **必须** 测试批量操作的 checkbox 选择和取消
- [ ] **必须** 测试批量取消后的状态变更
- [ ] **严禁** 使用真实任务引擎

参考: `.agent/memory/E2E_TESTING.md`
